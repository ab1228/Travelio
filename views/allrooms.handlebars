<div class="container">
    <section class="rooms">
        <h1>All Hotels</h1>

        {{#each }}
        <div class="room">
            <h3>{{}}</h3>
            <p>{{}}</p>
            <ul>
                <li>Beds: {{}}</li>
                <li><a href="#" class="btn btn-reverse">App</a></li>
            </ul>
            <div class="tech">
                <small>Price: $ <span>{{}}</span></small>
            </div>
        </div>

        {{else}}
        <p>No Hotel avalaible</p>

        {{/each}}

    </section>
</div>